<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BBIHardwareSupport.MDM.IntuneConfigManager.Views.OemConfigurationManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:BBIHardwareSupport.MDM.IntuneConfigManager.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">



    <StackPanel Padding="20">
        <TextBlock Text="OEMConfig Manager" FontSize="24" FontWeight="Bold" Margin="0 0 0 12"/>
        <Button Content="Find Olo Expo Config" Command="{Binding TestFindOemConfigCommand}" />
        <Button Content="Load OEM Configs" Command="{Binding LoadOemConfigsCommand}" />
<!--        <Button Content="Create Config for Restaurant" Click="CreateConfigForRestaurant_Click" Margin="0,20,0,0"/> -->
        <ListView ItemsSource="{Binding OemConfigs}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <TextBlock Text="{x:Bind}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
